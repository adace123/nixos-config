config:
  nixos-keys:host-configs:
    - name: common
      sops:
        encryptedPath: hosts/secrets.yaml
        rawPath: hosts/secrets-raw.yaml
    - name: iso
      sshUrl: root@iso.local
    - name: coruscant
      sops:
        encryptedPath: hosts/coruscant/secrets.yaml
        rawPath: hosts/coruscant/secrets-raw.yaml
      sshUrl: root@coruscant.local
